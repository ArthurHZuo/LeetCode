class Solution(object):
    def isPalindrome(self, x):
        """
        :type x: int
        :rtype: bool
        """
        if x < 0: return False
        rev = int(str(x)[::-1])
        return True if rev - x ==0 else False
